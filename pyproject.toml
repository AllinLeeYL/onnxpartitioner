[project]
name = "onnxpartitioner"
version = "0.1.0"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[project.scripts]
onnxpartitioner = "onnxpartitioner.partitioner:main"